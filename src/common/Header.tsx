import React, {FC} from 'react';
// import {useLocation} from 'react-router-dom';

const Header: FC = () => {
  // const location = useLocation();

  // return <MainNavigation location={location} />;
  return <div></div>;
};

export default Header;
