import styled from 'styled-components';

import {elementsGreyColor, offset120} from 'constants/styleVars';

export const Devider = styled.div`
  border-top: 1px solid ${elementsGreyColor};
  margin: ${offset120} 0;
`;
